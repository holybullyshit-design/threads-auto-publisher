# 스레드 자동화 프로그램 — Claude Code 작업 지침

이 파일은 세션마다 자동으로 로드된다. **매번 사용자가 다시 설명하지 않아도 되게, 여기 있는
규칙은 예외 없이 그대로 지킬 것.** 규칙을 바꾸고 싶으면 코드만 고치지 말고 이 파일도 같이
갱신한다 — 그래야 다음 세션(나 자신 포함)이 옛날 규칙으로 되돌아가지 않는다.

## 프로젝트 개요

- 로컬 경로: `~/Desktop/자동화/스레드 자동화 프로그램`, GitHub: `holybullyshit-design/threads-auto-publisher`
- Node/Express 로컬 앱 + GitHub Actions(5분마다)로 클라우드에서도 예약 발행됨 — 맥북이 꺼져 있어도 동작
- 예약 데이터는 로컬 파일이 아니라 **GitHub Contents API로 직접** 읽고 쓴다(`server/lib/githubStore.js`) — `schedule/posts.json`을 로컬에서 직접 편집하지 말 것, 항상 `readSchedule`/`writeSchedule`/`atomicUpdateSchedule`을 통해서만 쓴다.
- 계정 6개: 사주 5개(연리지실타래/아해사주/팔자명가/팔자궤도/팔자장인) + 파트너스 1개(엄마가 직접 써본 꿀템)
- 다른 세션(Codex 등)이 동시에 같은 저장소를 건드릴 수 있다 — **커밋 전엔 항상 `git pull --no-rebase --no-edit origin main`** 먼저 하고, 겹치는 작업 파일은 함부로 덮어쓰지 않는다.

## 사주 Threads 5개 계정 — 예약 시간 규칙 (2026-09-02 확정)

하루 5개, 계정마다 아래 규칙을 **예외 없이** 지킨다:

1. **첫 글은 오전 07:00~09:00 사이**
2. **글과 글 사이 간격은 최소 2시간 ~ 최대 3시간** (이게 최우선 규칙 — 21시 마감이나 저녁 코어 커버 같은 다른 목표보다 이 간격 규칙이 항상 이긴다. 산수상 다 같이는 못 만족하니, 충돌하면 항상 간격을 지킨다)
3. **매일 새로 랜덤 계산** — 하나의 고정 패턴을 여러 날 재사용하지 않는다(기계적으로 보이면 안 됨)
4. **연리지실타래 / 아해사주**: 하루 5개 중 1개는 "댓글유도 글"(생년월일시 남기는 상담체)이고, 이건 **완전히 다른 경로에서 생성**된다 — 이 프로그램의 시간 배치 스크립트가 절대 이 글의 시각을 건드리지 않는다. 대신 이 글을 "앵커(장애물)"로 취급해서, 나머지 4개(바이럴)를 배치할 때 앵커와 2시간 미만으로 가까워지면 앵커+2시간으로 건너뛰어 피해간다.

**구현**: `tools/fill-schedule.js`의 `generateDailySlots(count, anchorMin)` 함수가 이 규칙 전체를 담당한다. 시간대를 다시 손댈 일이 있으면 여기부터 보고, `tools/rebalance-daily-times.js`(이미 예약된 기존 글의 시각을 같은 규칙으로 재배치하는 도구)도 반드시 같이 맞춘다 — 두 파일의 `generateDailySlots`는 항상 동일해야 한다.
단, `fill-schedule.js`가 **실제로 빈 칸 시각을 정하는 건 `planDayTimes`**다(2026-09-15 개정) — 그 날 이미 있는 글(댓글유도 앵커 포함) 시각을 전부 고정점으로 두고, 07~09시 시작 / 모든 간격 2시간 이상 / 22시 이전을 만족하는 조합을 무작위로 찾는다. 예전 `dayLabels[already + i]` 방식은 기존 글이 오후에만 있는 부분일에서 새 글을 밤·새벽으로 밀어내는 사고가 있었다(13:17 예정 → 21:03 저장). 이 방식으로 되돌리지 말 것. `--dry-run`을 붙이면 시각 계획만 출력한다.

## 우리 채널에서 실제로 터진 글 공식 (2026-09-23 실측, 계정당 110여 건 추적 기준)

**남의 채널보다 이걸 먼저 본다.** 아래는 우리 계정에서 실제로 조회가 터진 글들의 공통 구조다. 새 글을 기획할 때 이 패턴을 우선 적용한다.

### 전 계정 공통 공식 (상위 글 대부분이 이 중 2~3개를 겹쳐 쓴다)
1. **띠 3개 묶음 + 출생연도 괄호 병기** — 가장 강력하다. 팔자장인 18,374회(`<이 세 띠, 도화살 TOP 3다>` 호랑이띠(1998·1986·1974) 식), 팔자궤도 11,119회(돼지·토끼·양 역마살), 팔자궤도 7,937회(1977·1989·2001년생 뱀띠…), 팔자명가 4,344회. 띠만 쓰지 말고 **연도를 괄호로 같이 적는다** — 독자가 2초 안에 자기를 찾는다.
2. **2단 판별(띠로 좁히고 → 조건으로 다시 좁히기)** — "이 셋 아니면 아예 해당도 안 됩니다" → "근데 이 셋이라고 다 붙는 건 아니에요. 원국에 이게 있어야만 성립합니다(묘 자리)". 1차 지목으로 끌어들이고 2차 조건을 클리프행어로 쓴다. 우리 최고 조회 글 2개가 전부 이 구조다.
3. **순위형(TOP 3 / 자리별 1~3위)에 역순 공개** — 팔자장인 1위 글(18,374), 연리지 1위 글(18,539) 둘 다 순위 구조이고 3위→1위로 올라간다.
4. **통념 뒤집기** — "삼재가 와도 **오히려 풀리는** 사주"(7,937), "삼재 온다는 말에 겁부터 먹는 사람 집중해"(7,004). 나쁘다고 알려진 것을 좋은 쪽으로 뒤집으면 저장·공유가 붙는다.
5. **길신(좋은 것) 소재가 흉살 공포보다 세다** — 천을귀인(연리지 18,539·팔자장인 5,496), 도화살 매력(18,374). 겁주는 글보다 "나한테 좋은 게 있다"가 더 터진다.
6. **양면 구조로 갈라 보여주기** — "원국에서 힘 있게 자리잡으면 매력으로 쓰이고 / 흔들리는 상태에서 겹치면 관계가 꼬이는 소모전이 된다". 한쪽으로 단정하지 않는다.
7. **사과형 도입("죄송하지만,")** — 팔자장인 7,558·5,489회, 팔자궤도 4,232회. 경고 톤의 진입이 잘 먹힌다.
8. **추상적 성격어 대신 생활 장면** — "단톡방에서 유독 반응이 먼저 옵니다", "헤어지고 연락처 지웠다가도 결국 다시 저장하게 되고".

### 계정별로 더 잘 먹히는 결
- **팔자장인**: 띠 TOP3 + 연도 / 일주 4개 직접 지목("경진·경술·임진·임술", 5,068회) / 사과형 도입. 조회는 높지만 답글은 잘 안 달린다.
- **팔자궤도**: 삼재·역마 같은 **시기성 소재를 통념 뒤집기로** 쓰는 게 최고 성과(좋아요 90). A/B 대조형도 잘 나온다(5,396회).
- **연리지 실타래**: 신살을 **연애 체감으로 번역**해야 터진다(천을귀인 → "같이 사는 사람 자체가 귀인", 18,539회). 반말 유지. 댓글이 폭발한 글은 따로 있다 — "이혼 얘기까진 안 갔는데, 집에 들어가기 싫어지는 사람"(답글 105) 처럼 **명리 용어 없이 감정 상황만 묘사**한 글이다.
- **아해사주**: 조회보다 **댓글이 터지는 계정**이다(답글 87·50·41건). 공식은 하나 — **부모의 걱정거리를 제목에 그대로 쓰고 → 그게 문제가 아니라 타고난 기질이라고 재해석(안심)**. 예: "또래보다 늦되어 보이는데 어느 순간 훌쩍 앞서가는 아이"(3,649회·답글 87), "친구는 많은데 정작 마음 나눌 친구가 없어 보이는 아이"(답글 50), "집에서만 유독 대드는 아이"(답글 41). 아이 칭찬형("어딜 가나 친구가 먼저 다가옵니다")은 조회는 나오지만 댓글은 덜 붙는다.
- **팔자명가**: TOP3 띠 + "한 번 더 생각해보세요" 경고형이 상위였다(2026-09-04부터 노출 제한 상태라 최근 데이터는 참고 불가).

### 반대로 성적이 나빴던 것
- 돈·이번 달 운세 같은 **시기형 소재**(평균 666회 vs 지목형 1,317회) → `LOW_PERFORMING_TOPIC_IDS`로 빈도를 낮춰둠.
- 명리 용어를 설명 없이 먼저 던지는 글, 띠·연도 지목이 없는 일반론.

## 벤치마킹 1순위 채널 (고정)

**@taebaek_saju(사주장인, 팔로워 5만)이 사용자가 지정한 1순위 기준 채널이다.** 벤치마킹 요청이 오면 로테이션 목록보다 이 채널을 먼저, 글 본문+답글 전체+댓글+프로필/고정글까지 깊게 본다(겉핥기 금지).

2026-09-22 실측으로 엔진에 반영한 것:
- **명리 용어 즉시 해설 1줄**(필수 규칙): "내 일간은 태어난 날의 첫 글자예요" 류. 용어를 모르는 독자 이탈을 막는 장치.
- **CTA 직전 "정보 갭" 2~3줄**(필수 규칙): 태어난 날/시간/대운 중 무엇이 왜 더 필요한지 명시 → 상담 필요성을 납득시킴. "정확한 건 원국을 봐야 한다" 식 막연한 한 줄로 대체 금지.
- **단일 행동 처방 클로징(`singleAction`)**: 팁 3개 나열이 아니라 "이번 달 안에 하나만 하세요" + 행동 1개.
- **표현 베끼기 금지(2026-09-22 사용자 지시)**: 벤치마크 채널의 구조·기획은 배우되 문장을 그대로 쓰면 표절 시비가 붙는다. `taebaekSajuDraftWriter.js`의 `BANNED_BENCHMARK_PHRASES`에 그 채널 고유 표현("저를 찾아오십시오", "내 일간은 태어난 날의 첫 글자", "일간만으로는 시기까지", "OO 깐 사람" 등)을 넣고, 생성물에 들어가면 실패 처리해 다시 쓰게 한다. 용어 해설·정보 갭도 고정 문장이 아니라 매번 다른 표현으로 쓰게 지시한다. 명리학 판별법 같은 사실과 파트 구성 형식 자체는 자유롭게 쓴다.
- 아직 안 한 것: 프로필 **고정글**(상담 상품 구성 + 실제 후기 모음). @taebaek_saju는 이 고정글 하나가 조회 114.5만이고, 전환의 핵심 자산이다. 우리 6계정 전부 고정글이 없다.

## 소재/발행량 조정 (2026-09-22 실측 반영)

- **저조 소재 비중 축소**: 돈·이번 달 운세 같은 "시기형"(`sipseong-jaeseong`/`sipseong-gwanseong`)은 글당 평균 666회, 띠·일주·신살 지목형은 1,317회(2배)였다. `taebaekSajuDraftWriter.js`의 `LOW_PERFORMING_TOPIC_IDS`로 시기형 등장 빈도를 1/3로 낮췄다(비중 15.4%→5.7%). 성적이 바뀌면 이 집합을 다시 조정한다 — 완전히 빼지는 말 것(소재 다양성 유지).
- **팔자명가는 하루 2개**(임시): 2026-09-04부터 노출 제한이 18일 이상 지속(글당 평균 조회 9회)되어, 9/22부터 남은 예약을 하루 5개 → 2개(오전 1 + 저녁 1)로 줄였다(27건 삭제). 회복이 확인되면 다시 5개로 채운다. 다른 4계정은 하루 5개 그대로.

**빈 슬롯 채우기**: `node tools/fill-schedule.js viral-morning "<연리지 실타래|아해사주>" <종료일>` / `node tools/fill-schedule.js comprehensive "<팔자명가|팔자궤도|팔자장인>" <종료일>` — 계정 5개를 **동시에 병렬로 실행하지 말 것**(같은 스케줄 파일에 쓰기 충돌 위험, 실제로 이 때문에 순차 실행으로 바꿈). 하나 끝나고 다음.

## 절대 하지 말 것 (실제로 사고 났던 것들)

1. **재배치 스크립트가 "이미 지난 시각"을 예약에 넣지 않게 할 것.** 오늘처럼 하루가 반쯤 지난 날짜를 재배치할 때, 새로 뽑은 시각이 현재 시각보다 과거일 수 있다 — 이걸 그대로 넣으면 다음 5분 주기 실행 때 그 글이 곧바로(다른 계정 것과 함께) 발행돼버린다. **실제로 5개 계정 전부에서 발생한 사고**(2026-09-02, 15:00~15:42 사이 여러 계정 글이 1~13분 간격으로 몰아서 게시됨, 이미 게시되어 되돌릴 수 없었음). `tools/rebalance-daily-times.js`는 새 시각이 `Date.now() + 15분`보다 이르면 그 글을 건드리지 않고 넘어가도록 이미 고쳐져 있다 — 이 안전장치를 제거하지 말 것.
2. **schedule/posts.json이 1MB를 넘으면 GitHub Contents API가 `content` 필드를 비워서 응답한다** (공식 제약, 1MB 초과 파일은 Git Blobs API를 써야 함). `server/lib/githubStore.js`의 `readSchedule()`이 이미 이 경우를 감지해서 Blobs API로 대신 읽도록 고쳐져 있다 — 이 분기를 지우지 말 것. 예약이 계속 쌓이니 이 문제는 다시 나타날 수 있다(파일이 더 커지면).
3. **"예약된 시간표"만 확인하고 끝내지 말 것.** `scheduledAt`(의도)이 멀쩡해 보여도 실제로 몇 시에 게시됐는지(`publishedAt`, 실제 결과)는 다를 수 있다. 예약/시간 배치 관련 작업을 검증할 땐 **반드시 최근 `status: published`인 글들의 실제 `publishedAt` 간격도 확인**한다 — 그래야 이미 발행된 사고를 놓치지 않는다.
4. **같은 write 스크립트를 실수로 두 번 연달아 실행하지 말 것.** (`| head`와 `| tail`을 따로 파이핑하려다 스크립트를 두 번 돌린 적 있음 — 매번 새 랜덤값을 쓰는 스크립트라 결과가 꼬인다) 결과를 여러 각도로 보고 싶으면 한 번 실행한 출력을 변수/파일에 저장해서 재사용한다.
5. **파괴적이거나 대량으로 스케줄을 바꾸는 스크립트는 새로 만들 때마다 `--dry-run`으로 먼저 미리보기.** (`tools/fix-time-collisions.js`, `tools/rebalance-daily-times.js`, `tools/purge-canceled.js` 전부 `--dry-run` 지원)
6. **취소(canceled) 상태 글은 완전 삭제한다** (soft-cancel로 남겨두지 않음) — `tools/purge-canceled.js` 참고. status만 바뀌고 레코드가 남아있으면 캘린더 상세 목록에 계속 보여서 혼동을 준다.

## 스케줄 변경 후 검증 체크리스트

### 팔자명가 Instagram 릴스 시간 검수 규칙 (2026-09-03 확정)

- 팔자명가 릴스는 고급 명조형 8초·2화면(정보 4초 + 댓글/DM 행동유도 4초)으로 만든다.
- 배경음은 `public/instagram-cards/assets/audio/reality-meta-sound-collection.mp4`를 사용한다. Meta Sound Collection 원본을 보관하며, 8초 구간에 0.3초 페이드인·0.55초 페이드아웃과 -19 LUFS 정규화를 적용한다. 최종 MP4에 AAC 48kHz 스테레오 음원 트랙이 실제로 있는지 검수한다.
- 여성 시청자가 실제로 결정하고 고민하는 관계·재회·돈·직장·가족·주거·건강·자기성장 소재를 중심으로 설계한다. 다만 성별 고정관념, 공포 조장, 결과 보장은 금지한다. 같은 예약 범위에서는 주제군이 한쪽에 몰리지 않았는지도 검사한다.
- 하루 3개이며 기본 KST 슬롯은 07:30 / 15:30 / 21:30이다. 작업 당일 오전 슬롯이 이미 지났다면 남은 시각을 점심·퇴근·야간으로 분산하고 모두 현재보다 15분 이상 뒤인지 검사한다. 2026-09-03 교체분은 12:45 / 17:30 / 21:30으로 확정했다.
- 예약을 쓰기 직전과 쓴 직후에 날짜별 정확히 3개인지, 시각 중복이 없는지, 과거 시각이 없는지 검사한다. 검사를 통과하지 못하면 예약을 쓰거나 발행하지 않는다.
- 제목·topicId·댓글 키워드는 같은 예약 범위에서 중복 금지. 생년과 띠의 실제 대응을 코드로 검사한다.
- 기존 예약 교체는 platform=instagram, accountKey=palja, mediaType=REELS, status=scheduled만 대상으로 한다. 이미 발행된 릴스와 팔자명가 카드, 연리지 실타래, Threads 예약은 건드리지 않는다.
- 공개 캡션의 #자동게시_* 같은 내부 관리 표식은 무조건 차단한다.
- 마지막 확인은 캘린더 표시만 보지 말고 원격 스케줄을 다시 읽어 날짜·시각·상태 30건을 재검산한다.

시간/예약 관련 작업을 했으면 커밋 전에 반드시:

```
node -e "
require('dotenv').config();
const { readSchedule } = require('./server/lib/githubStore');
(async()=>{
  const {posts} = await readSchedule();
  const accounts=['연리지 실타래','아해사주','팔자명가','팔자궤도','팔자장인'];
  const now=Date.now();
  let bad=0;
  for(const acct of accounts){
    const list=posts.filter(p=>p.accountLabel===acct&&p.status==='scheduled'&&p.platform!=='instagram'&&p.scheduledAt).map(p=>new Date(p.scheduledAt).getTime()).sort((a,b)=>a-b);
    for(let i=1;i<list.length;i++){
      const gapMin=(list[i]-list[i-1])/60000;
      if(gapMin < 119){ console.log('간격 위반:', acct, new Date(list[i-1]).toISOString(),'|',new Date(list[i]).toISOString(), gapMin.toFixed(1)+'분'); bad++; }
    }
  }
  const overdue = posts.filter(p=>accounts.includes(p.accountLabel)&&p.status==='scheduled'&&p.platform!=='instagram'&&p.scheduledAt&&new Date(p.scheduledAt).getTime()<now).length;
  console.log('2시간 미만 간격 위반:', bad, '건 / 이미 지난 예약:', overdue, '건');
})();
"
```

둘 다 0건이어야 정상. 위반이 있으면 커밋하지 말고 원인부터 찾는다.

## npm test

코드(스킬/엔진/스토어 등) 관련 작업 후엔 `npm test` 통과 확인 후 커밋한다(55개 테스트, 전부 통과해야 함).
