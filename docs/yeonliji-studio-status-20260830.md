# 2026-08-30 작업 상태

- 새 작업실 실행 주소: http://localhost:4322/
- 기존 4321 서버는 구 코드이며, 이 실행 환경에서 종료 권한이 거부되어 유지했다. 바탕화면 앱을 새 서버로 정상 재시작하는 문제는 아직 해결되지 않았다.
- 새 작업실: Instagram 작업실 → 연리지 실타래 → 주제 선택 → 이미지 생성 → 7장 미리보기 → 날짜/시간 지정 → 검수 승인 → 예약.
- 실제 OpenAI 이미지 생성 1회 성공. 시안 `yeonliji-2026-08-30-destiny-familiar-52a80d08`은 예약/게시하지 않았다. 초기 시안이라 이후 수정된 해시태그와 5·6장 제목 규칙은 반영되지 않았다.
- 브라우저 확인: 7장 로딩, 승인 전 예약 불가, 승인 후 활성화, 날짜 변경 시 승인 해제. 실제 예약 버튼은 누르지 않았다.
- 기획은 현재 26개. 생성된 주제/기존 게시 제목을 제외하고 최대 10개 추천하며 카테고리를 교차한다. 무한 소재 생성이나 의미상 중복 제로를 보장하지 않는다.
- 신규 캡션은 해당 카드의 이야기·용어·질문에 맞춘다. 해시태그는 브랜드+분야+사주 용어로 구성한다. 기존 시안/예약 문구를 소급 수정하지 않는다.
- 이미지 자동 검사는 규격/완전 동일 이미지/파일 변경 검사다. 캐릭터 일관성과 비슷한 구도, 오타·흐름은 사람의 최종 검수가 필요하다. 실제 샘플은 실내 장면 구도가 유사하므로 승인 전에 확인해야 한다.

## 공개 내부 태그 긴급 수정

- 원인: `cloud/publish-scheduled.js`가 공개 캡션에 `#자동게시_계정_예약ID`를 추가했다.
- 두 계정 공통 발행 경로에서 추가를 제거했다. 저장된 본문에 이 태그가 있다면 발행할 때 제거하며, 최종 Instagram 클라이언트도 내부 태그를 거부한다.
- 이전 태그 또는 고객용 캡션 일치로 기존 게시물을 확인한다.
- 실제 게시 전에 publishing 상태를 원격 저장한다. 게시 결과가 불명확하면 자동 재게시하지 않고 수동 확인 상태로 남긴다. 이 경우 publishedId와 실제 계정을 대조하기 전 scheduled로 되돌리지 말 것.
- 클라우드 배포 커밋: `e946bbfe0e92a92ed94b3d70b4579f000b13a879`. 발행 관련 3파일+테스트 1파일만 배포. 로컬의 기타 UI/런처 변경은 이 커밋에 포함하지 않았다.
- 배포 전후 예약 345건의 id/날짜/이미지/본문/dedupeKey 해시 동일: `6ac2e974829b40a499707a2866c57fef03b05fbd2c61d6c601c4491f86924c1b`.
- 기존 게시물 삭제/편집, 테스트 게시, 예약 재등록, 수동 발행 워크플로 실행은 하지 않았다.
- npm test: 26개 통과. 실제 발행 테스트는 외부 쓰기 없는 mock으로 수행했다.
- 기존 local HEAD와 원격 main이 다르다. 다음 작업 시 원격 커밋을 확인하고 로컬 변경을 보존할 것. 무조건 전체 push/reset 금지.
